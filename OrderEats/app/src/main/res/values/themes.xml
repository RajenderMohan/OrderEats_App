<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OrderEats" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Enable Activity Transitions -->
        <item name="android:windowActivityTransitions">true</item>

        <!-- Optional: Specify a custom shared element transition (create this file in res/transition/) -->
        <!--
        <item name="android:windowSharedElementEnterTransition">@transition/custom_shared_element_transition</item>
        <item name="android:windowSharedElementExitTransition">@transition/custom_shared_element_transition</item>
        -->

        <!-- Optional: Specify default enter/exit transitions for activities if not using shared elements extensively -->
        <!--
        <item name="android:windowEnterTransition">@android:transition/fade</item>
        <item name="android:windowExitTransition">@android:transition/fade</item>
        -->
    </style>

    <style name="Theme.OrderEats" parent="Base.Theme.OrderEats" />

    <style name="CustomSearchViewQueryText" parent="Widget.AppCompat.SearchView">
        <item name="android:editTextColor">@android:color/black</item>
        <item name="android:textColorHint">@android:color/darker_gray</item> <!-- Optional: for hint color -->
    </style>
</resources>
